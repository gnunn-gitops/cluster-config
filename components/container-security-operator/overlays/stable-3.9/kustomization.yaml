resources:
- ../../base

patches:
  - patch: |-
      - op: replace
        path: /spec/channel
        value: 'stable-3.9'
    target:
      kind: Subscription
      name: container-security-operator
