apiVersion: config.openshift.io/v1
kind: OAuth
metadata:
  name: cluster
spec:
  identityProviders:
  - htpasswd:
      fileData:
        name: htpass-secret
    mappingMethod: claim
    name: htpasswd
    type: HTPasswd
  - name: glauth
    mappingMethod: claim
    type: LDAP
    ldap:
      attributes:
        id:
        - dn
        name:
        - cn
        preferredUsername:
        - uid
      bindDN: "openshift,dc=ocplab,dc=com"
      bindPassword:
        name: ldap-secret
      insecure: false
      url: "ldap://proxy-server:389/dc=ocplab,dc=com?uid"
